# Flicker

How many things can one person operate through a single screen and how might the UI look like?

Flicker is one such UI that attempts to answer this question. It splits an operator's attention across multiple contexts by rapidly switching between them.

## Run

```
pnpm install
pnpm dev
```
